This change uses the project’s **design split**:

- `openspec/changes/llm-wiki/solution-architecture.md`: solution overview, boundaries, before→after, and diagrams
- `openspec/changes/llm-wiki/solution-analysis.md`: implementation-facing detail (files/symbols), data shapes, risks, testing strategy

This `design.md` file is intentionally brief so OpenSpec marks the **design** artifact complete.

### Archive reference

For examples of prior completed work and how changes are ultimately organized, see `openspec/changes/archive/` (for example `openspec/changes/archive/2026-04-19-implement-local-doc-ai-mcp/`). Once implemented and archived, this change will also land under `openspec/changes/archive/` with its finalized artifacts.

