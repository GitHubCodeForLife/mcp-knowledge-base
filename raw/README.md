# raw/

This folder contains **immutable source documents** (articles, PDFs, notes captured externally).

The system (and any LLM agent) may **read** these files, but MUST NOT modify them. All synthesis and maintenance lives in `wiki/`.

