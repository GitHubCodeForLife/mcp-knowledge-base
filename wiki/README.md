# wiki/

This folder contains the **LLM-maintained wiki layer**: interlinked markdown pages that summarize and synthesize the immutable raw sources under `raw/`.

Required entry points:
- `index.md`: content-oriented catalog of wiki pages
- `log.md`: append-only timeline of ingests/queries/lint passes

